# 企业大模型公共服务平台

## （模拟版）系统架构设计文档

*AI Gateway Console · Usage Collector · Monitoring Dashboard*

版本：V1.2

日期：2026年8月

适用场景：面试任务 / Agent 辅助开发

---

## 一、项目背景与需求

### 1.1 原始需求

基于中科大大模型公共服务平台（https://llm.ustc.edu.cn）打造企业自有大模型公共服务平台。

- 后端模拟生成调用模型 API 消耗 Token 的相关数据并存入数据库（如 Qwen / DeepSeek / MiniMax / GLM 等）
- 前端呈现基础信息并实时监控数据增长变化（以 3-5 个模型为例）
- 体现 UI 设计能力，合理设计并实现前端页面信息呈现与布局，不可照搬中科大平台
- 推荐项目架构：React/Vue3 + Go/Gin/Gorm + MySQL/Redis
- 思考后续接入校方统一身份认证的设计方案
- 无需提交项目 Demo 源代码，录制 5-10 分钟讲解视频，体现设计思路与实现结果以及优化方向

### 1.2 项目定位

本项目定位为「轻量级企业大模型公共服务平台」（AI Gateway Console）。

核心能力包括：

- 多模型统一管理与状态监控
- API 调用全链路日志采集
- Token 消耗统计分析与实时展示
- 平台健康状态与异常感知
- 权限与认证体系的可扩展设计

架构抽象：AI Gateway + Usage Collector + Monitoring Console。即使当前为模拟环境，数据模型与分层也按未来真实接入大模型 API 网关的方向设计。

### 1.3 Demo 实现范围说明（V1.2 新增）

本项目是面试演示项目，时间与人力有限。明确区分「实际实现」与「架构设计」，既保证 Demo 可落地演示，又体现企业级系统设计的完整思考。

#### Demo 实际实现（必须完成）

| 模块 | 内容 | 说明 |
|---|---|---|
| **API Gateway** | POST /api/chat 完整闭环 | 参数校验 → API Key 校验 → 额度检查 → 模型路由 → 调用采集 |
| **Mock Model Service** | 模拟模型回复 + Token 生成 | 5 个模型差异化特征，返回 OpenAI 兼容格式 |
| **统计采集** | 调用日志写入 + Redis 实时聚合 | 每次调用写 MySQL 日志 + 更新 Redis 指标 |
| **成本计算** | 每次调用计算费用 | 基于模型定价的实时费用统计 |
| **统计 API** | 全部 6 个 API 接口 | overview / model-usage / trend / models / logs / health |
| **前端 Dashboard** | 完整单页控制台 | 健康条 + 指标卡片 + 模型卡片 + 实时日志 + 趋势图 |
| **前端调用测试区** | 简易 API 调用测试面板 | 可现场演示一次 POST /api/chat 调用 |
| **API Key 安全** | bcrypt 存储 + 校验 | 完整 Key 生命周期管理 |
| **轻量额度检查** | Key 级日 Token 上限 | 超限返回 429 |
| **Docker Compose** | 一键启动 | MySQL + Redis + Server + Web |

#### 架构设计但暂不实现（面试时讲方案即可）

| 内容 | 处理方式 |
|---|---|
| **CAS / OIDC 统一身份认证** | 本章第十节给出完整设计方案，Demo 仅用简单 JWT 登录（可 mock） |
| **消息队列（Kafka / RabbitMQ）** | 文档第七章说明演进路径，Demo 用前端轮询代替 |
| **完整 RBAC 后台管理页面** | 文档定义角色权限矩阵，Demo 不实现用户/角色/菜单管理 UI |
| **多租户隔离** | 数据模型已预留 project → api_key 隔离链路 |
| **日志表分区 / 归档** | 文档 3.3 给出分阶段策略，Demo 单表即可 |
| **完整告警规则引擎** | 仅做健康状态展示，不实现告警通知 |
| **真实调用外部大模型 API** | 全部使用 Mock 模拟数据 |

> **面试价值**：这种「边界清晰」的设计思路恰恰体现你对项目周期和技术债务的理解——知道企业系统该怎么设计，也知道 Demo 阶段该做到什么程度。

---

## 二、总体架构

### 2.1 逻辑架构

系统采用分层架构，核心调用链如下：

```
用户 / 业务系统
    │
    ▼
API Gateway（统一入口：鉴权、路由、限流）
    │
    ▼
模型路由层 → Qwen / DeepSeek / MiniMax / GLM / Qwen-Coder
    │
    ▼
Usage Collector（调用数据采集）
    │
    ┌─────┴─────┐
    ▼           ▼
  Redis       MySQL
（实时指标）  （历史日志）
    │
    ▼
Vue3 Dashboard（监控与管理控制台）
```

### 2.2 API Gateway 边界说明（V1.2 新增）

**Demo 阶段实现方式：** 以 Gin 中间件 + Handler 组合实现轻量 Gateway，**不引入独立网关组件**（如 Kong / APISIX）。所有 Gateway 职责收敛于 Go 后端进程内。

Gateway 负责的职责：

| 职责 | 实现方式 | 说明 |
|---|---|---|
| **参数校验** | Gin middleware | 校验必填字段、参数类型与取值范围 |
| **API Key 校验** | Gin middleware | 从 Header 提取 Key，查表校验有效性（详见第九章） |
| **额度检查** | Gin middleware | 查询 Redis 当日已用 Token，超限返回 429（详见 5.3） |
| **模型路由** | Handler 内逻辑 | 根据请求中指定的 model 字段路由到对应模拟逻辑 |
| **调用采集** | Handler 内逻辑 | 每次调用完成后异步写入 api_usage_logs 并更新 Redis 指标 |
| **限流（Demo 简化）** | 可选 middleware | Demo 阶段可仅做 Key 级简单计数，生产再扩展令牌桶/漏桶 |
| **成本计算** | Handler 内逻辑 | 根据模型定价计算本次调用费用，写入日志 + 更新 Redis |

**生产演进方向：** 当需要独立扩缩容、多实例部署或更复杂的路由策略时，可将 Gateway 拆分为独立服务，或在前面挂 Kong/APISIX 做统一入口。

### 2.3 技术选型与理由

| 层级 | 技术选型 | 选型理由 |
|---|---|---|
| **前端** | Vue3 + Vite + Element Plus + ECharts | 组件成熟、企业后台风格统一、图表能力强、开发效率高 |
| **后端** | Go + Gin + Gorm | 高并发友好、适合网关类服务、Gorm 开发效率高 |
| **主数据库** | MySQL 8.x | 成熟稳定，适合存储用户、项目、调用日志等结构化数据 |
| **缓存** | Redis | 实时聚合指标缓存，避免 Dashboard 高频查询打到 MySQL |
| **实时方案** | 轮询（Demo）/ WebSocket（可扩展） | Demo 阶段轮询实现简单可靠；生产可升级消息队列 + WebSocket/SSE |
| **认证** | JWT（当前）+ OAuth2/OIDC/CAS（未来） | 当前快速落地，未来平滑对接学校统一身份认证 |

### 2.4 项目目录结构（V1.2 新增）

#### 后端目录结构 (Go)

```
server/
├── cmd/
│   └── server/
│       └── main.go              # 入口：初始化配置、DB、Redis、启动 HTTP Server
├── internal/
│   ├── config/
│   │   └── config.go            # 配置结构体 + 加载逻辑（YAML/ENV）
│   ├── model/
│   │   ├── user.go              # User Gorm 模型
│   │   ├── project.go           # Project Gorm 模型
│   │   ├── api_key.go           # ApiKey Gorm 模型
│   │   ├── model.go             # Model（大模型）Gorm 模型
│   │   └── usage_log.go         # ApiUsageLog Gorm 模型
│   ├── handler/
│   │   ├── auth.go              # 登录/注册 Handler
│   │   ├── chat.go              # POST /api/chat 核心 Handler
│   │   ├── model.go             # GET /api/models
│   │   ├── stats.go             # 统计数据相关 Handler
│   │   ├── health.go            # GET /api/health
│   │   └── log.go               # GET /api/realtime/logs
│   ├── service/
│   │   ├── auth_service.go      # 认证业务逻辑（JWT 签发/校验）
│   │   ├── chat_service.go      # 模拟调用 + 采集逻辑
│   │   ├── stats_service.go     # 统计聚合查询
│   │   └── model_service.go     # 模型状态管理
│   ├── simulator/
│   │   └── simulator.go         # 独立 goroutine：定时生成模拟调用数据
│   ├── middleware/
│   │   ├── auth.go              # JWT 校验中间件
│   │   ├── api_key.go           # API Key 校验中间件
│   │   ├── cors.go              # 跨域处理
│   │   └── logger.go            # 请求日志记录
│   └── router/
│       └── router.go            # 路由注册与分组
├── migrations/
│   └── 001_init.sql             # 初始化建表 SQL
├── config.yaml                  # 默认配置文件
├── Dockerfile                   # 后端容器化
├── go.mod
└── go.sum
```

#### 前端目录结构 (Vue3)

```
web/
├── public/
├── src/
│   ├── api/
│   │   ├── index.ts             # axios 实例，拦截器（注入 Token）
│   │   ├── stats.ts             # 统计相关 API 封装
│   │   ├── models.ts            # 模型相关 API 封装
│   │   └── logs.ts              # 日志相关 API 封装
│   ├── composables/
│   │   ├── usePolling.ts        # 通用轮询 Hook（可配置间隔）
│   │   └── useCountUp.ts        # 数字增长动画 Hook
│   ├── components/
│   │   ├── layout/
│   │   │   ├── HealthBar.vue    # 顶部健康状态条
│   │   │   └── SideNav.vue      # 侧边导航
│   │   ├── dashboard/
│   │   │   ├── MetricCards.vue  # 核心指标卡片行
│   │   │   ├── ModelCards.vue   # 模型卡片列表
│   │   │   ├── RealtimeLog.vue  # 实时调用流表格
│   │   │   └── TrendChart.vue   # 7 天趋势折线图
│   │   ├── common/
│   │   │   ├── StatusDot.vue    # 状态指示灯（绿/黄/红）
│   │   │   └── TokenBar.vue     # Token 进度条
│   │   └── charts/
│   │       ├── LineChart.vue    # ECharts 折线图封装
│   │       └── PieChart.vue     # ECharts 饼图/环形图封装
│   ├── router/
│   │   └── index.ts             # Vue Router 配置
│   ├── stores/
│   │   └── dashboard.ts         # Pinia Store：仪表盘全局状态
│   ├── types/
│   │   └── index.ts             # TypeScript 类型定义
│   ├── App.vue
│   └── main.ts
├── Dockerfile                   # 前端容器化
├── nginx.conf                   # 生产静态文件 Nginx 配置
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

### 2.5 业务调用时序图（V1.2 新增）

以下展示一次 `POST /api/chat` 请求的完整调用链路，比架构图更贴近系统实际运行过程：

```
用户 / 前端调用测试区
    │
    │  POST /api/chat  (Authorization: Bearer sk-xxx)
    ▼
┌─ API Gateway 层 ─────────────────────────────────────────┐
│  1. 参数校验 middleware     → 校验 model、messages 必填   │
│  2. API Key 校验 middleware → bcrypt 比对，定位 project   │
│  3. 额度检查 middleware     → Redis GET quota:key:{id}:*  │
│     (超限 → 429 Too Many Requests)                       │
└──────────────────────────────────────────────────────────┘
    │  (放行)
    ▼
┌─ Model Router 层 ────────────────────────────────────────┐
│  4. 根据 model 字段匹配 models 表                         │
│     (模型不存在/已下线 → 404)                             │
└──────────────────────────────────────────────────────────┘
    │
    ▼
┌─ Mock Model Service 层 ──────────────────────────────────┐
│  5. 生成模拟回复内容 + Token 用量数据                     │
│     - prompt_tokens (基于输入消息长度估算)                │
│     - completion_tokens (随机 80~500)                     │
│     - latency_ms (按模型特征随机)                          │
│     - status (按模型稳定性随机 success/fail/timeout)      │
└──────────────────────────────────────────────────────────┘
    │
    ▼
┌─ Usage Collector 层 ─────────────────────────────────────┐
│  6a. 计算费用:                                           │
│      cost = (prompt_tokens/1000)×input_price              │
│           + (completion_tokens/1000)×output_price         │
│  6b. INSERT INTO api_usage_logs (含 cost 字段)            │
│  6c. 异步更新 Redis 实时指标 (INCRBY + EXPIRE):           │
│      - stats:{date}:total_tokens                         │
│      - stats:{date}:total_requests                       │
│      - stats:{date}:total_cost                           │
│      - stats:{date}:model:{id}:tokens                    │
│      - quota:key:{key_id}:{date}:tokens (额度计数)        │
└──────────────────────────────────────────────────────────┘
    │
    ▼
┌─ 响应 ───────────────────────────────────────────────────┐
│  返回 OpenAI 兼容格式 JSON (含 usage + cost)              │
└──────────────────────────────────────────────────────────┘

同时，前端 Dashboard 每 3~5 秒轮询:
  GET /api/stats/overview  → 从 Redis 读取 → 指标卡片实时刷新
  GET /api/realtime/logs   → 从 MySQL 读取 → 调用日志滚动更新
```

> 面试时这张图比架构图更好讲——跟着一次请求走完全链路，每个环节都能展开说设计考量。

---

## 三、数据库设计

### 3.1 ER 关系说明

核心实体关系：User → Project → API Key → Model → Usage Log。

一个用户可拥有多个项目；一个项目可创建多个 API Key；每次调用记录关联项目、API Key 与模型。

### 3.2 表结构详细定义

#### 3.2.1 users（用户表）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO | 主键 |
| username | VARCHAR(64) | UNIQUE, NOT NULL | 用户名 / 学工号 |
| display_name | VARCHAR(64) | | 显示名称 |
| password_hash | VARCHAR(256) | NOT NULL | bcrypt 密码哈希（V1.2 补充） |
| role | VARCHAR(32) | NOT NULL | 角色：admin / teacher / student |
| department | VARCHAR(128) | | 院系 / 部门 |
| status | TINYINT | DEFAULT 1 | 1=正常, 0=禁用 |
| created_at | DATETIME | | 创建时间 |
| updated_at | DATETIME | | 更新时间 |

> **V1.2 补充 `password_hash` 字段**：用于 JWT 登录认证，使用 bcrypt 单向哈希存储。

#### 3.2.2 projects（项目表）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO | 主键 |
| name | VARCHAR(128) | NOT NULL | 项目名称 |
| owner_id | BIGINT | FK → users.id | 项目负责人 |
| description | VARCHAR(512) | | 项目描述 |
| status | TINYINT | DEFAULT 1 | 1=启用, 0=停用 |
| created_at | DATETIME | | 创建时间 |
| updated_at | DATETIME | | 更新时间 |

#### 3.2.3 api_keys（API 密钥表）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO | 主键 |
| project_id | BIGINT | FK → projects.id | 所属项目 |
| name | VARCHAR(64) | | 密钥备注名 |
| key_hash | VARCHAR(256) | NOT NULL | bcrypt 密钥哈希（V1.2 调整长度） |
| key_prefix | VARCHAR(16) | | 前缀展示（如 sk-xxxx） |
| status | TINYINT | DEFAULT 1 | 1=有效, 0=禁用 |
| last_used_at | DATETIME | | 最后使用时间 |
| created_at | DATETIME | | 创建时间 |

#### 3.2.4 models（模型表）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO | 主键 |
| name | VARCHAR(64) | UNIQUE, NOT NULL | 模型标识名（如 qwen2.5-72b） |
| display_name | VARCHAR(128) | | 展示名称 |
| provider | VARCHAR(32) | | 提供商：Qwen / DeepSeek / MiniMax / GLM |
| status | VARCHAR(16) | DEFAULT 'online' | online / offline / degraded |
| input_price | DECIMAL(10,6) | NOT NULL, DEFAULT 0 | 输入价格（元/1K tokens）（V1.2 新增） |
| output_price | DECIMAL(10,6) | NOT NULL, DEFAULT 0 | 输出价格（元/1K tokens）（V1.2 新增） |
| description | VARCHAR(256) | | 模型简介 |
| created_at | DATETIME | | 创建时间 |

> **V1.2 新增定价字段**：成本是企业最关心的指标。Demo 阶段使用参考定价（如 Qwen2.5-72B: 输入 ¥0.004/1K, 输出 ¥0.012/1K），生产环境接入真实 API 后可同步更新。

#### 3.2.5 api_usage_logs（调用日志表）

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO | 主键 |
| project_id | BIGINT | INDEX | 关联项目 |
| api_key_id | BIGINT | INDEX | 关联 API Key |
| model_id | BIGINT | INDEX | 关联模型 |
| prompt_tokens | INT | DEFAULT 0 | 输入 Token 数 |
| completion_tokens | INT | DEFAULT 0 | 输出 Token 数 |
| total_tokens | INT | DEFAULT 0 | 总 Token 数 |
| cost | DECIMAL(10,6) | DEFAULT 0 | 本次调用费用（元）（V1.2 新增） |
| latency_ms | INT | | 延迟（毫秒） |
| status | VARCHAR(16) | | success / fail / timeout |
| error_message | VARCHAR(256) | | 错误信息（失败时） |
| created_at | DATETIME | INDEX | 调用时间（重要索引） |

建议索引：

- `idx_created_at` — 按时间范围查询
- `idx_model_id_created_at` — 按模型 + 时间统计
- `idx_project_id_created_at` — 按项目 + 时间统计
- `idx_api_key_id_created_at` — 按 Key + 时间追溯（V1.2 补充）

#### 3.2.6 daily_usage_stats（日汇总表）（V1.2 新增）

趋势图如果长期直接扫 `api_usage_logs`，当数据量增长后性能会下降。引入轻量日汇总表，由后台 goroutine 在每日零点后（或定时）对前一日数据做一次聚合写入：

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO | 主键 |
| stat_date | DATE | NOT NULL | 统计日期 |
| model_id | BIGINT | INDEX | 关联模型（NULL=全局汇总） |
| total_tokens | BIGINT | DEFAULT 0 | 当日 Token 总数 |
| total_requests | INT | DEFAULT 0 | 当日请求数 |
| success_count | INT | DEFAULT 0 | 成功次数 |
| fail_count | INT | DEFAULT 0 | 失败次数 |
| total_cost | DECIMAL(12,6) | DEFAULT 0 | 当日总费用 |
| avg_latency_ms | INT | | 平均延迟 |

**唯一约束**：`UNIQUE (stat_date, model_id)`，每天每模型一条记录。

查询趋势时优先走此表：`SELECT * FROM daily_usage_stats WHERE stat_date BETWEEN ? AND ? ORDER BY stat_date`。Demo 阶段可先直接查 `api_usage_logs` 做 `GROUP BY DATE(created_at)`，数据量小不影响性能。

### 3.3 日志表长期数据策略（V1.2 新增）

`api_usage_logs` 是系统中写入量最大的表。生产环境建议：

| 阶段 | 量级 | 策略 |
|---|---|---|
| **Demo / POC** | 10 万级 | 单表即可，无需分区 |
| **初期生产** | 百万~千万级 | MySQL `RANGE` 分区，按月分区（`PARTITION BY RANGE (TO_DAYS(created_at))`），自动创建未来分区 |
| **中期** | 亿级 | 热数据（近 3 个月）留在分区表，冷数据按月归档到历史表或对象存储 |
| **长期** | 十亿级+ | 考虑接入 ClickHouse / Elasticsearch 等时序/搜索型存储 |

> 设计原则：当前 Demo 不做分区，但建表时 **不阻塞将来 `ALTER TABLE ... PARTITION BY RANGE` 的可行性**（主键需包含分区键，即 `PRIMARY KEY (id, created_at)`，或调整索引策略）。

---

## 四、后端设计

### 4.1 模拟数据生成策略

后台启动独立 goroutine，每隔 3~6 秒按权重随机选择模型生成一次调用记录。不同模型赋予差异化特征，使监控数据更接近真实生产环境：

| 模型 | 调用频率 | 单次 Token | 延迟特点 | 稳定性 |
|---|---|---|---|---|
| Qwen2.5-72B | 高 | 中等 | 中等 | 高 |
| DeepSeek-V3 | 中低 | 较高 | 较低 | 高 |
| MiniMax-Text-01 | 中 | 中高 | 较高 | 中 |
| GLM-4 | 中 | 中等 | 低 | 很高 |
| Qwen2.5-Coder | 中高 | 中等 | 中等 | 高 |

生成字段包括：prompt_tokens、completion_tokens、total_tokens、latency_ms、status（success/fail/timeout）、error_message。成功率整体控制在约 97%~99%。

### 4.2 模拟器生命周期管理（V1.2 新增）

```go
// simulator/simulator.go 核心思路

type Simulator struct {
    ctx    context.Context
    cancel context.CancelFunc
    db     *gorm.DB
    rdb    *redis.Client
}

func (s *Simulator) Start() {
    go func() {
        ticker := time.NewTicker(randomInterval(3*time.Second, 6*time.Second))
        defer ticker.Stop()
        for {
            select {
            case <-s.ctx.Done():
                log.Println("[Simulator] gracefully stopped")
                return
            case <-ticker.C:
                s.generateOne()
                ticker.Reset(randomInterval(3*time.Second, 6*time.Second))
            }
        }
    }()
}

func (s *Simulator) Stop() {
    s.cancel() // 触发 ctx.Done()，goroutine 退出
}
```

关键点：

- 使用 `context.WithCancel` 管理 goroutine 生命周期，`main.go` 中监听 OS 信号（SIGINT/SIGTERM）调用 `Stop()`
- 每次执行后 `Reset` ticker，实现随机间隔
- 生成数据时**先写 MySQL，再异步更新 Redis**（Redis 更新失败不影响主流程）

### 4.3 核心 API 接口

| 接口路径 | 方法 | 说明 |
|---|---|---|
| POST /api/auth/login | POST | 用户登录，返回 JWT |
| POST /api/chat | POST | 模拟大模型调用（核心业务接口） |
| GET /api/models | GET | 模型列表及当前状态 |
| GET /api/stats/overview | GET | 核心概览：今日/累计 Token、请求数、成功率、平均延迟 |
| GET /api/stats/model-usage | GET | 各模型今日 Token 用量与占比 |
| GET /api/stats/trend | GET | 近 7 天 Token 与请求量趋势 |
| GET /api/realtime/logs | GET | 最近 N 条调用日志（支持分页） |
| GET /api/health | GET | 平台健康检查 |

### 4.4 核心 API 请求/响应 Schema（V1.2 新增）

#### 4.4.1 POST /api/auth/login

**Request:**

```json
{
    "username": "zhangsan",
    "password": "xxxxxxxx"
}
```

**Response (200):**

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "token": "eyJhbGciOiJIUzI1NiIs...",
        "expires_at": "2026-08-06T12:00:00Z",
        "user": {
            "id": 1,
            "username": "zhangsan",
            "display_name": "张三",
            "role": "student"
        }
    }
}
```

**Response (401):**

```json
{
    "code": 401,
    "message": "用户名或密码错误",
    "data": null
}
```

---

#### 4.4.2 POST /api/chat（V1.2 重点补充）

**Request Headers:**

```
Authorization: Bearer sk-a1b2c3d4e5f6g7h8
Content-Type: application/json
```

**Request Body:**

```json
{
    "model": "qwen2.5-72b",
    "messages": [
        { "role": "system", "content": "你是一个有帮助的助手。" },
        { "role": "user", "content": "介绍一下量子计算" }
    ],
    "temperature": 0.7,
    "max_tokens": 2048
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `model` | string | 是 | 模型标识名，需与 models 表 `name` 匹配 |
| `messages` | array | 是 | 对话消息列表，符合 OpenAI 兼容格式 |
| `temperature` | float | 否 | 采样温度 (0-2)，默认 1.0 |
| `max_tokens` | int | 否 | 最大输出 Token，默认 2048 |

**成功响应 (200):**

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "id": "chatcmpl-mock-abc123",
        "object": "chat.completion",
        "created": 1694000000,
        "model": "qwen2.5-72b",
        "choices": [
            {
                "index": 0,
                "message": {
                    "role": "assistant",
                    "content": "量子计算是一种利用量子力学原理进行信息处理的计算范式..."
                },
                "finish_reason": "stop"
            }
        ],
        "usage": {
            "prompt_tokens": 42,
            "completion_tokens": 156,
            "total_tokens": 198,
            "cost": 0.00204
        }
    }
}
```

> 设计意图：响应格式兼容 OpenAI Chat Completions API，便于现有 SDK/工具直接对接。

**常见错误响应:**

*API Key 无效 (401):*

```json
{
    "code": 401,
    "message": "无效的 API Key",
    "data": null
}
```

*日额度超限 (429) (V1.2 新增):*

```json
{
    "code": 429,
    "message": "当日 Token 额度已用尽，请明天再试或联系项目管理员提升额度",
    "data": {
        "quota_limit": 1000000,
        "quota_used": 1000000,
        "reset_at": "2026-08-06T00:00:00Z"
    }
}
```

*模型不存在 (404):*

```json
{
    "code": 404,
    "message": "模型 qwen2.5-72b 不存在或已下线",
    "data": null
}
```

*参数校验失败 (422):*

```json
{
    "code": 422,
    "message": "参数校验失败",
    "data": {
        "errors": [
            { "field": "model", "message": "model 为必填字段" },
            { "field": "messages", "message": "messages 不能为空" }
        ]
    }
}
```

*服务内部异常 (500):*

```json
{
    "code": 500,
    "message": "服务内部错误",
    "data": null
}
```

---

#### 4.4.3 GET /api/stats/overview（V1.2 重点补充）

**Response (200):**

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "today": {
            "total_tokens": 1245670,
            "total_requests": 3842,
            "success_rate": 0.983,
            "avg_latency_ms": 342,
            "total_cost": 18.52
        },
        "total": {
            "total_tokens": 892345678,
            "total_requests": 2784000,
            "total_cost": 12480.35
        },
        "trend": {
            "token_change_pct": 12.5,
            "request_change_pct": 8.3
        },
        "model_count": {
            "online": 4,
            "total": 5
        }
    }
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `today.total_tokens` | int | 今日 Token 消耗总数 |
| `today.total_requests` | int | 今日请求总数 |
| `today.success_rate` | float | 今日成功率 (0-1) |
| `today.avg_latency_ms` | int | 今日平均延迟（毫秒） |
| `today.total_cost` | float | **今日预计费用（元）**（V1.2 新增，核心指标） |
| `total.total_tokens` | int | 平台累计 Token 消耗 |
| `total.total_requests` | int | 平台累计请求数 |
| `total.total_cost` | float | 平台累计费用（元）（V1.2 新增） |
| `trend.token_change_pct` | float | 较昨日 Token 变化百分比（可为负） |
| `trend.request_change_pct` | float | 较昨日请求量变化百分比（可为负） |
| `model_count.online` | int | 当前在线模型数 |
| `model_count.total` | int | 平台接入模型总数 |

> 所有 `today.*` 数据来源于 Redis 实时聚合缓存；`total.*` 来源于 MySQL `SUM()` 查询（可缓存 5 分钟）；`trend.*` 通过对比前一日 Redis 快照或 MySQL 昨日数据得出。

---

#### 4.4.4 其他接口主要返回字段

**GET /api/models — 模型列表：**

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "models": [
            {
                "id": 1,
                "name": "qwen2.5-72b",
                "display_name": "Qwen2.5-72B",
                "provider": "Qwen",
                "status": "online",
                "description": "通义千问 2.5 72B 参数模型",
                "input_price": 0.004,
                "output_price": 0.012,
                "today_tokens": 452300,
                "today_requests": 1280,
                "avg_latency_ms": 380,
                "today_cost": 3.82
            }
        ]
    }
}
```

**GET /api/stats/model-usage — 模型用量分布：**

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "models": [
            { "model_name": "qwen2.5-72b", "tokens": 452300, "percentage": 0.36 },
            { "model_name": "qwen2.5-coder", "tokens": 321000, "percentage": 0.26 }
        ],
        "total_tokens": 1245670
    }
}
```

**GET /api/stats/trend — 7 天趋势：**

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "points": [
            { "date": "2026-07-30", "tokens": 1050000, "requests": 3200 },
            { "date": "2026-08-05", "tokens": 1245670, "requests": 3842 }
        ]
    }
}
```

**GET /api/realtime/logs — 实时调用日志（V1.2 加分页）：**

| Query 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `page` | int | 1 | 页码 |
| `page_size` | int | 20 | 每页条数（最大 100） |
| `model_id` | int | 可选 | 按模型过滤 |
| `status` | string | 可选 | success / fail / timeout |

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "logs": [
            {
                "id": 12039,
                "model_name": "qwen2.5-72b",
                "project_name": "智能问答系统",
                "total_tokens": 235,
                "cost": 0.00282,
                "status": "success",
                "latency_ms": 410,
                "created_at": "2026-08-05T14:32:15Z"
            }
        ],
        "pagination": {
            "page": 1,
            "page_size": 20,
            "total": 3842
        }
    }
}
```

**GET /api/health — 健康检查：**

```json
{
    "code": 0,
    "message": "ok",
    "data": {
        "db": "connected",
        "redis": "connected",
        "online_models": 4,
        "total_models": 5,
        "alerts": [
            {
                "level": "warning",
                "model": "minimax-text-01",
                "message": "模型状态为 degraded"
            }
        ]
    }
}
```

---

## 五、模型健康检测（V1.2 新增）

### 5.1 状态定义

models 表 `status` 字段有三个取值：

| 状态 | 含义 | Dashboard 指示 |
|---|---|---|
| `online` | 模型正常可用 | 绿色指示灯 |
| `degraded` | 模型可用但延迟升高或偶发失败 | 黄色指示灯 |
| `offline` | 模型不可用 | 红色指示灯 |

### 5.2 状态来源

**Demo 阶段：** 由 simulator/health_checker.go 定时（每 10~15 秒）根据最近调用数据评估状态：

```
func evaluateModelHealth(modelID uint, recentLogs []ApiUsageLog) string {
    // 取最近 N 条该模型的调用记录
    // success_rate = success / total
    // avg_latency = avg(latency_ms)
    //
    // if success_rate >= 0.95 && avg_latency < threshold  → online
    // if success_rate >= 0.80                                 → degraded
    // else                                                    → offline
}
```

**生产演进方向：** 替换为真实的健康探测——定期向模型 API 发送轻量探活请求，结合成功率、延迟、错误率综合判定，而非依赖历史调用数据。

> 面试要点：Demo 阶段状态不是"写死的"。基于后台 goroutine 定期分析最近调用记录自动更新状态，逻辑透明、可视，让面试官看到状态确实会动态变化。

### 5.3 初始状态与数据初始化

服务启动时，所有模型默认为 `online`。simulator 开始生成数据后，health_checker 根据实际数据动态调整。若某模型连续失败或延迟异常，状态会自动变为 `degraded` 或 `offline`——这正是 Dashboard 实时感的来源之一。

---

## 六、Redis 使用说明

### 6.1 设计原则

MySQL 负责历史调用记录的持久化存储；Redis 负责实时聚合指标的缓存，避免 Dashboard 高频刷新时反复扫描 MySQL。

### 6.2 缓存 Key 设计（V1.2 优化跨天策略）

采用 **日期后缀** 方案，避免 `stats:today:*` 跨天混淆问题：

```
# 格式: stats:{date}:{metric}
# {date} 使用 YYYYMMDD 格式，例 20260805

stats:20260805:total_tokens              # 今日总 Token
stats:20260805:total_requests            # 今日总请求数
stats:20260805:total_cost                # 今日总费用（元）（V1.2 新增）
stats:20260805:success_count             # 成功次数
stats:20260805:fail_count                # 失败次数
stats:20260805:timeout_count             # 超时次数
stats:20260805:total_latency_ms          # 累计延迟（用于计算平均延迟）
stats:20260805:model:{model_id}:tokens   # 各模型今日 Token
stats:20260805:model:{model_id}:requests # 各模型今日请求数
stats:20260805:model:{model_id}:latency_ms # 各模型累计延迟
stats:20260805:model:{model_id}:cost     # 各模型今日费用（V1.2 新增）

# 全局累计（无日期后缀，永久递增，定期从 MySQL 校准）
stats:total:total_tokens                 # 平台累计总 Token
stats:total:total_requests               # 平台累计总请求数
stats:total:total_cost                   # 平台累计总费用（V1.2 新增）
```

**跨天处理策略：**

- 后台 goroutine 每次生成数据时，使用 `time.Now().Format("20060102")` 拼接当日 Key
- 所有带日期的 Key 统一设置 **TTL = 48 小时**（`EX 172800`），过期自动清理
- Redis `INCRBY`/`HINCRBY` 原子操作更新，无需担心并发
- 平均成功率/延迟通过 success_count/(success+fail+timeout) 和 total_latency/total_requests **计算得出**，不是直接存储

### 6.3 额度管理与校验（V1.2 升级）

为支撑配额管理，每个 API Key 增加调用计数 Key：

```
quota:key:{key_id}:20260805:tokens      # 该 Key 当日 Token 消耗
quota:key:{key_id}:20260805:requests    # 该 Key 当日请求数
```

**Demo 阶段实现轻量额度校验：**

在 POST /api/chat 的中间件链路中，API Key 校验通过后，查询 Redis：

```
1. GET quota:key:{key_id}:{date}:tokens → 当日已用 Token
2. 从 api_keys 表或配置读取该 Key 的日配额上限（如默认 1,000,000 tokens/天）
3. if (已用 + 预估本次) > 上限 → 返回 429 + 提示配额已用完
4. 放行 → Handler 执行完成后 INCRBY 更新计数
```

**超限响应 (429)：**

```json
{
    "code": 429,
    "message": "当日 Token 额度已用尽，请明天再试或联系项目管理员提升额度",
    "data": {
        "quota_limit": 1000000,
        "quota_used": 1000000,
        "reset_at": "2026-08-06T00:00:00Z"
    }
}
```

实现成本极低（几行 Redis 命令），但能体现「配额管理」的企业思维，面试演示时主动触发展示 429 场景会很有说服力。

**生产演进方向：** 引入 Redis + Lua 脚本实现原子性「检查 + 扣减」，避免并发超限；配额配置从硬编码迁移到数据库 + 管理后台。

---

## 七、前端设计

### 7.1 设计目标

从普通「数据大屏」升级为「企业 AI 服务管理控制台」（AI Gateway Console）。强调信息层级清晰、实时感强、具备管理属性，而非单纯展示数字。

### 7.2 页面模块划分

**（1）顶部健康状态条**

- 模型在线数量 / 总模型数
- 异常模型提示（如有）
- 数据最后更新时间

**（2）核心指标卡片（5~6 个）**

- **今日预计费用 ¥xx.xx**（V1.2 升级为核心指标，带动画增长）
- 今日总 Token（带动画增长）
- 累计总 Token
- 今日请求数
- 平均成功率
- 平均延迟（ms）

**（3）模型卡片列表**

每个模型一张卡片，展示：

- 模型名称 + 状态指示灯（绿/黄/红）
- 今日 Token 用量 + 进度条
- 今日费用（V1.2 新增）
- 输入/输出定价（V1.2 新增）
- 平均延迟
- 成功率

**（4）实时调用流**

滚动表格或时间线，字段建议：时间 | 项目 | 模型 | Token | 费用 | 状态 | 延迟

**（5）API 调用测试区（V1.2 新增，强烈建议）**

在 Dashboard 页面底部或侧边提供一个简易调用测试面板，方便视频中现场演示一次真实的 POST /api/chat 调用：

- 模型下拉选择（自动加载 /api/models 列表）
- 消息输入框（预设示例问题，一键填入）
- 发送按钮 → 调用 POST /api/chat → 展示返回结果（回复内容 + Token 用量 + 费用）
- 调用成功后 Dashboard 指标卡片数字自动跳变（实时感最强的一刻）

这是视频演示的「高光时刻」——现场发一次请求，Token 和费用数字实时跳动，比任何架构图都有说服力。

**（6）分析图表区**

- 近 7 天 Token / 请求 / **费用** 趋势折线图（V1.2 增加费用趋势线）
- 模型使用占比（环形图或饼图，按 Token / 费用双维度切换）

### 7.3 UI 风格建议

- 浅色企业风，主色蓝/青系
- 卡片圆角 + 适度阴影，留白合理
- 数字变化使用平滑过渡动画，增强实时感
- 避免信息过载，重点突出「状态」与「变化」

---

## 八、实时监控方案

### 8.1 Demo 阶段

前端每 3~5 秒轮询 `/api/stats/overview` 与 `/api/realtime/logs`。实现简单、稳定，足够支撑演示。

可在 `composables/usePolling.ts` 中封装通用轮询逻辑：

```typescript
// usePolling 核心思路
// 1. 接受回调 fn 和间隔 interval
// 2. 组件 mounted 时启动 setInterval
// 3. 组件 unmounted 时 clearInterval
// 4. 支持 pause/resume（页面隐藏时暂停，减少无效请求）
```

### 8.2 生产演进方向

```
数据采集 → 消息队列（Kafka / RabbitMQ / Redis Stream）
         → WebSocket 或 SSE 推送
         → 前端实时更新
```

Redis 继续承担实时聚合指标的缓存职责，降低后端压力。

---

## 九、API Key 安全设计（V1.2 新增/强化）

### 9.1 Key 生成流程

```
1. 用户（或管理员）在项目中创建 API Key
2. 后端调用 crypto/rand 生成 32 字节随机密钥
3. 原始 Key 格式: sk-{base64url-encode(random_bytes)}
4. 保存 key_hash = bcrypt(原始Key, cost=12) 到 api_keys.key_hash
5. 保存 key_prefix = 原始Key的前 12 个字符 到 api_keys.key_prefix
6. 仅此一次将原始 Key 展示给用户，提示妥善保管
```

### 9.2 Key 校验流程

```
1. 请求到达 → API Key 中间件提取 Header "Authorization: Bearer sk-xxx"
2. 计算 key_prefix = 提取的 Key 前 12 字符
3. 查数据库: SELECT * FROM api_keys WHERE key_prefix = ? AND status = 1
4. 对匹配的行，使用 bcrypt.CompareHashAndPassword(key_hash, 提取的Key)
5. 匹配成功 → 注入 api_key 信息到 context，放行
6. 全不匹配 → 返回 401
```

### 9.3 安全措施

| 措施 | 说明 |
|---|---|
| **bcrypt 哈希** | cost=12，防彩虹表、防暴力破解，即使数据库泄露也无法还原原始 Key |
| **前缀索引** | `key_prefix` 加速数据库检索，避免全表 bcrypt 比对 |
| **不明文记录** | 日志中仅输出 `key_prefix`，不输出原始 Key |
| **状态控制** | `status` 字段支持随时禁用/启用 Key |
| **最后使用时间** | `last_used_at` 用于识别僵尸 Key，支持自动清理策略 |

---

## 十、统一身份认证设计方案

### 10.1 当前 Demo 方案

采用简单 JWT 登录（可 mock 用户数据），快速验证权限控制流程。

JWT 设计要点：

- 签发时写入 `user_id`、`role`、`exp`
- Access Token 有效期 24 小时
- 前端存储在 localStorage，axios 拦截器自动注入 `Authorization` Header
- 后端 Gin middleware 校验 Token 有效性，注入用户信息到 `gin.Context`

### 10.2 未来正式方案

```
用户点击登录
    ↓
跳转学校统一身份认证中心（CAS / OAuth2 / OIDC）
    ↓
认证成功，回调平台后端，获取用户信息（学工号、姓名、院系、角色）
    ↓
平台自动创建 / 绑定本地用户，下发 JWT
    ↓
基于 RBAC 的权限控制
```

### 10.3 角色与权限建议

| 角色 | 权限范围 |
|---|---|
| 管理员 | 管理模型上下线、项目管理、全局统计查看、用户管理 |
| 教师 | 管理自己的项目与 API Key、查看项目级统计 |
| 学生 | 使用已分配的 API Key 进行调用、查看个人调用记录 |

### 10.4 安全补充

- JWT 过期与刷新机制（Access Token 24h + Refresh Token 7d）
- 操作审计日志（谁创建了 Key、谁调用了多少 Token）
- 防 CSRF、接口限流（`rate` middleware）
- 敏感操作二次确认

---

## 十一、本地开发与部署（V1.2 新增）

### 11.1 Docker Compose 一键启动

```yaml
# docker-compose.yml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: ai_gateway
    ports:
      - "3306:3306"
    volumes:
      - ./server/migrations:/docker-entrypoint-initdb.d

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  server:
    build: ./server
    ports:
      - "8080:8080"
    environment:
      DB_DSN: root:root123@tcp(mysql:3306)/ai_gateway?charset=utf8mb4&parseTime=True
      REDIS_ADDR: redis:6379
    depends_on:
      - mysql
      - redis

  web:
    build: ./web
    ports:
      - "3000:80"
    depends_on:
      - server
```

启动命令：`docker-compose up -d`

### 11.2 本地开发模式

```bash
# 后端
cd server
cp config.example.yaml config.yaml   # 修改数据库连接信息
go run cmd/server/main.go

# 前端
cd web
npm install
npm run dev                           # Vite 开发服务器 :5173
```

---

## 十二、视频讲解建议（5~10 分钟）

1. **项目定位与 Demo 范围**（约 30 秒）— "这不是数据看板，是企业 AI Gateway Console。Demo 实现了什么、什么只是架构设计，边界清晰"

2. **总体架构与技术选型**（约 1 分钟）— 展示逻辑架构图 + 调用时序图，讲清楚 Gateway → Router → Collector → Dashboard 链路

3. **数据库设计与企业级抽象**（约 1 分钟）— User → Project → API Key → Model → Usage Log 链路，强调成本字段和日汇总表的设计考量

4. **现场演示：POST /api/chat 闭环调用**（约 1.5 分钟，核心高光时刻）
   - 在调用测试区发送一次真实请求
   - 展示返回的 OpenAI 兼容格式结果（含 Token + 费用）
   - Dashboard 指标卡片数字实时跳动
   - 实时日志中出现刚才的调用记录

5. **模拟数据生成与实时效果**（约 1.5 分钟）— 模拟器差异化特征、后台自动生成、Dashboard 实时刷新

6. **API Key 安全与额度校验**（约 1 分钟）— bcrypt 存储、前缀索引、额度超限 429 演示

7. **成本统计**（约 30 秒）— 为什么把费用做成核心指标、模型定价表展示

8. **统一身份认证与未来扩展**（约 1 分钟）— JWT → CAS/OIDC 演进路径、明确哪些只是设计方案

演示重点：**闭环调用 + 数字实时跳动 + 设计边界清晰**。功能不求多，实时感与设计深度最重要。争取让面试官看完后说"这个确实是按企业标准来思考的"。

---

## 十三、优化与扩展方向

- 真实接入大模型 API 网关（替换模拟生成逻辑）
- 完善 API Gateway 能力：鉴权、限流、路由、熔断
- 多租户隔离与配额管理
- 成本统计与账单模拟
- 异常告警（Token 突增、失败率升高、模型离线）
- 操作审计与合规日志
- 消息队列 + WebSocket 实现真正实时推送
- 对接学校统一身份认证（CAS/OAuth2/OIDC）

---

## 十四、方案亮点总结

相比普通数据看板，本方案重点体现：

- **闭环思维**（Demo 实现范围明确，不堆砌功能——知道什么该做、什么该只讲设计）
- 企业级系统抽象（User → Project → API Key → Model → Usage）
- 大模型调用链路理解（Gateway → Router → Collector，附完整时序图）
- API 设计的兼容性思维（OpenAI 兼容格式 + 独立管理 API）
- **成本即核心**（费用统计不是"可选功能"，而是模型定价 → 实时计算 → 今日费用卡片 → 趋势分析的完整闭环）
- API Key 全生命周期安全管理（bcrypt 哈希 → 前缀索引 → 额度校验 → 429 限流）
- **模型健康自动感知**（基于调用数据的状态自动评估，不是写死的绿/黄/红）
- 数据架构能力（MySQL 持久化 + Redis 实时聚合 + 日汇总表 + 分区归档策略）
- 权限与安全设计（JWT → 统一身份认证 + RBAC，Demo 落地 + 生产方案并存）
- 可扩展性思考（从 Demo 到生产的演进路径，每处都有"现在怎么做 → 未来怎么升级"）
- 工程化意识（目录分层、Docker Compose、goroutine 生命周期管理）

目标：打造一个轻量级、可演示、可讲解、可扩展的企业 AI 服务管理平台雏形。**闭环 > 功能数量，设计深度 > 代码行数。**

---

> 文档结束
