# 项目分阶段开发计划

> 项目名称：企业大模型公共服务平台（AI Gateway Console）
>
> 技术栈：Go + Gin + Gorm + MySQL + Redis / Vue3 + Vite + Element Plus + ECharts
>
> 架构文档：[企业大模型公共服务平台系统架构设计文档 V1.2](企业大模型公共服务平台系统架构设计文档%20V1.2.md)

---

## 总体原则

```
先设计，再编码
先闭环，再扩展
每一步可运行
不过度设计
```

---

## 阶段总览

| 阶段 | 名称 | 优先级 | 核心产出 | 验收标准一句话 |
|---|---|---|---|---|
| 0 | 开发计划确认 | P0 | 本文档 | 计划评审通过 |
| 1 | 项目初始化与基础环境 | P0 | Go/Vue 骨架 + Docker Compose | `docker-compose up` 启动 MySQL/Redis |
| 2 | 数据库设计与初始化 | P0 | 建表 SQL + GORM Model + 种子数据 | 6 张表可查，5 个模型带定价 |
| 3 | 后端基础能力 | P0 | Gin 启动 + DB/Redis + 中间件 + /api/health | `GET /api/health` 返回 200 |
| 4 | **核心调用闭环** | **P0** | POST /api/chat 完整链路 | 一次调用 → MySQL 有日志 + Redis 有指标 + 返回 OpenAI 格式 |
| 5 | 统计接口开发 | P1 | 6 个 Dashboard API | overview / model-usage / trend / logs / models / health 全部正确 |
| 6 | Vue Dashboard 开发 | P1 | 完整单页控制台 + 调用测试区 | 页面可演示、轮询刷新、现场闭环调用 |
| 7 | 系统完善与交付 | P2 | README / Docker 优化 / Demo 检查清单 | 正式可交付 |

---

## 阶段 0：开发计划确认

### 阶段目标

确认开发计划，建立开发契约。

### 本阶段内容

1. 输出本《项目分阶段开发计划》
2. 等待评审确认
3. 确认后进入阶段 1

### 验收标准

- 计划评审通过
- 阶段划分、优先级、验收标准无异议

### 涉及文件

无（不写代码）

---

## 阶段 1：项目初始化与基础环境

### 阶段目标

搭建项目骨架，Docker Compose 一键启动 MySQL + Redis。

### 实现内容

**1. Git 初始化**

- `git init`
- `.gitignore`（Go + Vue + IDE 通用模板）

**2. Go 项目初始化**

- `go mod init ai-gateway-server`
- 创建架构文档 [2.4 节] 定义的目录结构骨架（只有目录和空 `.go` 文件占位，不含业务代码）

```
server/
├── cmd/server/main.go          # 仅输出 "server starting..."
├── internal/
│   ├── config/config.go        # 配置结构体定义
│   ├── model/                  # 空包
│   ├── handler/                # 空包
│   ├── service/                # 空包
│   ├── simulator/              # 空包
│   ├── middleware/             # 空包
│   └── router/router.go       # 空路由注册
├── migrations/
│   └── 001_init.sql            # 空文件占位
├── config.yaml                 # 模板配置
├── Dockerfile                  # 应用 Dockerfile
├── go.mod
└── go.sum
```

**3. Vue3 项目初始化**

- Vite 脚手架创建项目
- 安装依赖：`element-plus`, `echarts`, `vue-echarts`, `pinia`, `vue-router`, `axios`
- 创建架构文档 [2.4 节] 定义的前端目录结构骨架

```
web/
├── src/
│   ├── api/           # axios 实例 + API 封装
│   ├── composables/   # usePolling / useCountUp
│   ├── components/    # layout / dashboard / common / charts
│   ├── router/        # Vue Router
│   ├── stores/        # Pinia Store
│   ├── types/         # TypeScript 类型
│   ├── App.vue
│   └── main.ts
├── Dockerfile
├── nginx.conf
├── package.json
├── vite.config.ts
└── tsconfig.json
```

**4. Docker Compose**

按架构文档 [11.1 节] 定义的 YAML 创建 `docker-compose.yml`：
- MySQL 8.0（端口 3306，自动执行 migrations 目录下 SQL）
- Redis 7-alpine（端口 6379）
- 后端服务（端口 8080）
- 前端服务（端口 3000）

**5. Go 依赖安装**

```bash
go get github.com/gin-gonic/gin
go get gorm.io/gorm
go get gorm.io/driver/mysql
go get github.com/redis/go-redis/v9
go get github.com/golang-jwt/jwt/v5
go get golang.org/x/crypto/bcrypt
go get gopkg.in/yaml.v3
go mod tidy
```

### 为什么这样设计

- 先搭建骨架，不写业务代码，确保目录结构符合架构文档
- Docker Compose 先行，基础设施从第一天就可用
- Go 和 Vue 项目各自独立，符合前后端分离原则

### 涉及文件

**新增：**

| 文件 | 说明 |
|---|---|
| `server/cmd/server/main.go` | 入口（仅打印启动信息） |
| `server/internal/config/config.go` | 配置结构体定义 |
| `server/internal/router/router.go` | 空路由注册 |
| `server/config.yaml` | 模板配置 |
| `server/Dockerfile` | 后端容器化 |
| `server/go.mod`、`go.sum` | Go 模块依赖 |
| `server/migrations/001_init.sql` | 空占位 |
| `web/` 整个目录 | Vue3 项目脚手架 |
| `docker-compose.yml` | 基础设施编排 |
| `.gitignore` | Git 忽略规则 |

### 数据结构变化

无

### API 变化

无

### 启动方式

```bash
# 一键启动所有基础设施
docker-compose up -d

# 后端开发模式
cd server && go run cmd/server/main.go

# 前端开发模式
cd web && npm install && npm run dev
```

### 验收标准

- [ ] `docker-compose up -d` 成功启动 MySQL 8.0 + Redis 7
- [ ] `go run cmd/server/main.go` 输出 "server starting..."
- [ ] `npm run dev` 启动 Vite 开发服务器
- [ ] `docker-compose down` 正常关闭

---

## 阶段 2：数据库设计与初始化

### 阶段目标

把架构文档第三章完整落库。所有表结构、索引、种子数据就位。

### 实现内容

**1. 完整建表 SQL — `migrations/001_init.sql`**

6 张表：

| 表 | 说明 | 关键字段 |
|---|---|---|
| users | 用户表 | username, password_hash(bcrypt), role |
| projects | 项目表 | name, owner_id(FK→users) |
| api_keys | API 密钥表 | key_hash(bcrypt, VARCHAR 256), key_prefix(VARCHAR 16) |
| models | 模型表 | name, provider, input_price(DECIMAL), output_price(DECIMAL), status |
| api_usage_logs | 调用日志表 | project_id, api_key_id, model_id, prompt_tokens, completion_tokens, total_tokens, cost, latency_ms, status, created_at |
| daily_usage_stats | 日汇总表 | stat_date, model_id, total_tokens, total_requests, success_count, fail_count, total_cost, avg_latency_ms |

每张表包含：
- 正确的字段类型和约束
- 索引（`idx_created_at`, `idx_model_id_created_at`, `idx_project_id_created_at`, `idx_api_key_id_created_at`）
- 唯一约束（users.username, models.name, daily_usage_stats(stat_date, model_id)）

**2. GORM Model 定义 — `internal/model/`**

| 文件 | 对应表 |
|---|---|
| `user.go` | users |
| `project.go` | projects |
| `api_key.go` | api_keys |
| `model.go` | models |
| `usage_log.go` | api_usage_logs |
| `daily_stats.go` | daily_usage_stats |

每个 Model 包含：
- GORM tag（column, type, default, index, uniqueIndex）
- TableName() 方法
- 必要的关联定义（BelongsTo / HasMany）

**3. 种子数据 — `migrations/002_seed.sql`**

用户（3 个）：

| username | password（明文，代码中 bcrypt 哈希） | role |
|---|---|---|
| admin | admin123 | admin |
| teacher_wang | teacher123 | teacher |
| student_zhang | student123 | student |

项目（1 个）：
- 名称：`AI 智能问答系统`，owner = admin

API Key（1 个）：
- Key 原始值：`sk-example-key-for-demo-20260805`（bcrypt 哈希后存储）
- 关联到上述项目

模型（5 个）+ 定价：

| name | provider | input_price | output_price |
|---|---|---|---|
| qwen2.5-72b | Qwen | 0.004 | 0.012 |
| deepseek-v3 | DeepSeek | 0.002 | 0.008 |
| minimax-text-01 | MiniMax | 0.015 | 0.060 |
| glm-4 | GLM | 0.050 | 0.050 |
| qwen2.5-coder | Qwen | 0.004 | 0.012 |

### 为什么这样设计

- 数据库是整个系统的地基，建表 SQL 即 "schema as code"，可重复执行
- GORM Model 为后续所有代码提供类型安全的数据访问
- 种子数据让项目从阶段 2 结束后就能连接数据库看到有效数据

### 涉及文件

| 文件 | 操作 |
|---|---|
| `server/migrations/001_init.sql` | 新增：完整建表 DDL |
| `server/migrations/002_seed.sql` | 新增：种子数据 |
| `server/internal/model/user.go` | 新增 |
| `server/internal/model/project.go` | 新增 |
| `server/internal/model/api_key.go` | 新增 |
| `server/internal/model/model.go` | 新增 |
| `server/internal/model/usage_log.go` | 新增 |
| `server/internal/model/daily_stats.go` | 新增 |

### 数据结构变化

建立 6 张表，每条记录的结构严格按架构文档第三章定义。

### API 变化

无

### 测试方案

```bash
# 1. 启动 MySQL
docker-compose up -d mysql

# 2. 执行建表
docker exec -i mysql mysql -u root -proot123 ai_gateway < server/migrations/001_init.sql

# 3. 执行种子数据
docker exec -i mysql mysql -u root -proot123 ai_gateway < server/migrations/002_seed.sql

# 4. 验证
docker exec -it mysql mysql -u root -proot123 ai_gateway
> SHOW TABLES;                       # 应显示 6 张表
> SELECT id, name, provider, input_price, output_price FROM models;  # 5 行
> SELECT id, username, role FROM users;  # 3 行
> SELECT id, name, key_prefix FROM api_keys;  # 1 行
> DESC api_usage_logs;                # cost 字段应为 DECIMAL(10,6)
```

### 验收标准

- [ ] 6 张表全部创建成功
- [ ] 所有索引和唯一约束生效
- [ ] 种子数据可正常查询
- [ ] models 表 input_price / output_price 字段正确（DECIMAL(10,6)）
- [ ] api_usage_logs 表 cost 字段正确

---

## 阶段 3：后端基础能力

### 阶段目标

Go Gin 服务启动、配置管理、DB/Redis 连接、基础中间件、健康检查接口。

### 实现内容

**1. 配置管理 — `internal/config/config.go`**

- YAML 文件加载 + 环境变量覆盖（DB_DSN、REDIS_ADDR）
- 结构体：ServerConfig、DatabaseConfig、RedisConfig、JWTConfig
- 默认值：端口 8080、日志级别 info

**2. Gin 引擎启动 — `cmd/server/main.go`**

```
加载配置 → 连接 MySQL(Gorm) → 连接 Redis(go-redis) → 注册中间件 → 注册路由 → 启动 HTTP Server
```

- 优雅关闭：监听 SIGINT/SIGTERM，依次关闭 HTTP Server、Redis 连接、MySQL 连接

**3. 路由注册 — `internal/router/router.go`**

- 全局中间件：CORS → Logger → Recovery
- 基础路由组：`/api/health`（不做认证）
- 为后续阶段预留 API 路由组

**4. 中间件 — `internal/middleware/`**

| 文件 | 功能 |
|---|---|
| `cors.go` | 允许前端跨域：`Access-Control-Allow-Origin: *`，支持 OPTIONS 预检 |
| `logger.go` | 请求日志：方法、路径、耗时(ms)、状态码、客户端 IP |
| 内置 Recovery | Gin 自带，捕获 panic 返回 500 |

**5. 统一响应格式**

所有 API 响应统一为：

```json
{
    "code": 0,          // 0=成功, 非0=错误
    "message": "ok",    // 可读消息
    "data": {}          // 业务数据
}
```

封装到 `internal/handler/response.go` 中，所有 handler 复用。

**6. 健康检查 — `internal/handler/health.go`**

`GET /api/health`：
- 检查 MySQL 连接（`db.Raw("SELECT 1")`）
- 检查 Redis 连接（`rdb.Ping()`）
- 统计 models 表 online 模型数
- 返回 alerts（当前空数组，阶段 4/5 增强）

### 为什么这样设计

- Gin、Gorm、Redis 的初始化是本阶段重点，之后每个阶段都叠加其上
- 中间件「先铺好轨道再跑火车」，后续 API Key 校验、JWT 校验只需新增中间件并注册
- 统一响应格式从第一天就建立，避免后续接口格式不一致

### 涉及文件

| 文件 | 操作 |
|---|---|
| `server/cmd/server/main.go` | **修改**：完整启动逻辑 + 优雅关闭 |
| `server/internal/config/config.go` | **修改**：完整配置加载 |
| `server/internal/router/router.go` | **修改**：中间件 + 路由注册 |
| `server/internal/middleware/cors.go` | **新增** |
| `server/internal/middleware/logger.go` | **新增** |
| `server/internal/handler/response.go` | **新增**：统一响应工具函数 |
| `server/internal/handler/health.go` | **新增** |
| `server/config.yaml` | **修改**：完整配置内容 |

### 数据结构变化

无（使用阶段 2 的表）

### API 变化

| 接口 | 方法 | 状态 | 认证 |
|---|---|---|---|
| GET /api/health | GET | **新增** | 否 |

### 测试方案

```bash
# 1. 确保 MySQL + Redis 运行
docker-compose up -d mysql redis

# 2. 启动后端
cd server && go run cmd/server/main.go

# 3. 测试健康检查
curl http://localhost:8080/api/health

# 预期响应
{
    "code": 0,
    "message": "ok",
    "data": {
        "db": "connected",
        "redis": "connected",
        "online_models": 5,
        "total_models": 5,
        "alerts": []
    }
}

# 4. 测试 CORS（预检请求）
curl -X OPTIONS http://localhost:8080/api/health \
  -H "Origin: http://localhost:5173" \
  -H "Access-Control-Request-Method: GET" \
  -v
# 预期响应头包含 Access-Control-Allow-Origin

# 5. 测试优雅关闭（在终端按 Ctrl+C）
# 预期日志输出：[Server] gracefully shutting down...
```

### 验收标准

- [ ] `go run cmd/server/main.go` 启动无报错
- [ ] MySQL 连接成功（启动日志确认）
- [ ] Redis 连接成功（启动日志确认）
- [ ] `GET /api/health` 返回 200，db 和 redis 均为 "connected"
- [ ] CORS 头正常返回
- [ ] Ctrl+C 优雅关闭（无连接泄漏报错）
- [ ] 统一错误响应格式验证（访问不存在的路径返回 JSON 格式 404）

---

## 阶段 4：核心调用闭环（最高优先级）

### 阶段目标

实现 POST /api/chat 完整链路。这是整个项目的**核心验收阶段**——一次调用后：

1. 返回 OpenAI 兼容格式结果
2. 数据库产生调用记录（含费用）
3. Redis 产生统计数据
4. 模拟器后台持续生成数据

### 实现内容

**整体调用链路：**

```
POST /api/chat (Authorization: Bearer sk-xxx)
  │
  ▼
[参数校验 middleware] → 校验 model 必填、messages 必填、非空
  │
  ▼
[API Key 校验 middleware] → key_prefix 查询 + bcrypt 比对 → 注入 context
  │
  ▼
[额度检查 middleware] → Redis GET quota:key:{id}:{date}:tokens → 超限返回 429
  │
  ▼
[Chat Handler] → 参数绑定 → 调用 Chat Service
  │
  ▼
[Chat Service / Mock Model] → 匹配模型 → 生成模拟回复 + Token 用量 + 时延 + 费用
  │
  ▼
[Usage Collector] → 同步写入 api_usage_logs → 异步 goroutine 更新 Redis 指标
  │
  ▼
返回 OpenAI 兼容格式 JSON (含 usage.cost)
```

**1. API Key 校验中间件 — `internal/middleware/api_key.go`**

```
校验流程：
1. 提取 Header "Authorization: Bearer sk-xxx"
2. 取前 12 字符作为 key_prefix
3. 查 DB: SELECT * FROM api_keys WHERE key_prefix = ? AND status = 1
4. bcrypt.CompareHashAndPassword(key_hash, 原始Key)
5. 成功 → 注入 api_key_id, project_id, key_id 到 context
6. 失败 → 返回 401
```

**2. 额度检查 — 合并到 API Key 校验中间件中**

```
1. Redis GET quota:key:{key_id}:{YYYYMMDD}:tokens
2. 默认日配额: 1,000,000 tokens/天（种子数据中可设）
3. 已用 + 预估 > 配额 → 返回 429
4. 放行
```

**3. Mock Model Service — `internal/service/chat_service.go`**

核心逻辑：

```go
func (s *ChatService) Chat(ctx context.Context, modelName string, messages []Message) (*ChatResponse, error) {
    // 1. 查 models 表匹配模型 → 不存在返回 404
    model := s.modelRepo.FindByName(modelName)
    
    // 2. 估算 prompt_tokens（基于消息长度 × 字符/token 比率）
    promptTokens := estimateTokens(messages)
    
    // 3. 按模型特征随机生成 completion_tokens（80~500）
    completionTokens := randomByModelProfile(model.Name, "tokens")
    
    // 4. 按模型特征随机生成 latency_ms
    latencyMs := randomByModelProfile(model.Name, "latency")
    
    // 5. 按模型稳定性随机 status
    status := randomStatus(model.Name) // success 97-99%
    
    // 6. 生成模拟回复内容（预设模板库按 topic 匹配）
    content := generateMockResponse(messages)
    
    // 7. 计算费用
    cost := float64(promptTokens)/1000*model.InputPrice + 
            float64(completionTokens)/1000*model.OutputPrice
    
    return &ChatResponse{...}, nil
}
```

**4. Usage Collector — 同文件 `chat_service.go`**

```go
func (s *ChatService) collect(ctx context.Context, resp *ChatResponse, projectID, keyID, modelID uint) {
    // 同步：写入 api_usage_logs
    log := &ApiUsageLog{
        ProjectID:        projectID,
        ApiKeyID:         keyID,
        ModelID:          modelID,
        PromptTokens:     resp.Usage.PromptTokens,
        CompletionTokens: resp.Usage.CompletionTokens,
        TotalTokens:      resp.Usage.TotalTokens,
        Cost:             resp.Usage.Cost,
        LatencyMs:        resp.LatencyMs,
        Status:           resp.Status,
        ErrorMessage:     resp.ErrorMessage,
    }
    s.db.Create(log)

    // 异步：更新 Redis 指标（失败不影响主流程）
    go s.updateRedisMetrics(ctx, log)
}
```

Redis 更新内容（全部原子操作 INCRBY + EXPIRE 172800）：

```
stats:{date}:total_tokens
stats:{date}:total_requests
stats:{date}:total_cost
stats:{date}:success_count / fail_count / timeout_count
stats:{date}:total_latency_ms
stats:{date}:model:{id}:tokens / requests / latency_ms / cost
quota:key:{key_id}:{date}:tokens / requests
stats:total:total_tokens / total_requests / total_cost
```

**5. Chat Handler — `internal/handler/chat.go`**

- 绑定请求体 → 结构体校验
- 从 context 获取 api_key_id、project_id（由中间件注入）
- 调用 chat_service.Chat()
- 格式化 OpenAI 兼容响应
- 错误映射：ErrModelNotFound → 404、ErrInvalidKey → 401、ErrQuotaExceeded → 429

**6. 模拟器 — `internal/simulator/simulator.go`**

- 使用 `context.WithCancel` 管理 goroutine 生命周期
- 每隔 3~6 秒随机间隔生成一次调用
- 按权重随机选择模型（Qwen2.5-72B 高频、DeepSeek-V3 中低频）
- 直接调用 chat_service（作为内部 API 调用，跳过 HTTP 层）
- 在 `main.go` 中启动：`simulator.Start()`
- 优雅关闭：监听 SIGINT/SIGTERM → `simulator.Stop()`

### 为什么这样设计

- 先写 MySQL（同步），再更新 Redis（异步）。Redis 挂了不影响 API 正常返回
- 模拟器复用 chat_service 的生成逻辑，保证数据特征一致
- OpenAI 兼容格式让后续可对接任何 OpenAI SDK/工具
- 对 bcrypt 的少量性能开销（~50ms/次），Demo 单机场景完全可接受

### 涉及文件

| 文件 | 操作 |
|---|---|
| `server/internal/middleware/api_key.go` | **新增**：API Key 校验 + 额度检查 |
| `server/internal/service/chat_service.go` | **新增**：Mock 生成 + 费用计算 + 日志写入 + Redis 更新 |
| `server/internal/handler/chat.go` | **新增**：POST /api/chat Handler |
| `server/internal/handler/response.go` | **修改**：增加 429 错误响应函数 |
| `server/internal/simulator/simulator.go` | **新增**：后台模拟数据生成 |
| `server/internal/router/router.go` | **修改**：注册 `/api/chat` + API Key 中间件 |
| `server/cmd/server/main.go` | **修改**：启动模拟器 goroutine |
| `server/config.yaml` | **修改**：增加额度默认配额配置 |

### 数据结构变化

使用阶段 2 的全部 6 张表。无新表。

### API 变化

| 接口 | 方法 | 状态 | 认证 |
|---|---|---|---|
| POST /api/chat | POST | **新增** | API Key (Bearer sk-xxx) |

请求/响应 Schema 严格遵循架构文档 [4.4.2 节]。

### 测试方案

```bash
# === 前提：MySQL + Redis 已启动，种子数据已写入 ===
docker-compose up -d mysql redis
# 确认已执行 migrations/001_init.sql 和 002_seed.sql

# === 启动后端 ===
cd server && go run cmd/server/main.go

# === 1. 基础成功调用 ===
curl -s -X POST http://localhost:8080/api/chat \
  -H "Authorization: Bearer sk-example-key-for-demo-20260805" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen2.5-72b",
    "messages": [
      {"role": "user", "content": "你好，介绍一下量子计算"}
    ]
  }' | jq .

# 预期：
# - code: 0
# - data.choices[0].message.content 有回复内容
# - data.usage 含 prompt_tokens, completion_tokens, total_tokens, cost
# - data.model = "qwen2.5-72b"

# === 2. 验证 MySQL ===
docker exec -it mysql mysql -u root -proot123 ai_gateway \
  -e "SELECT id, model_id, total_tokens, cost, status, created_at FROM api_usage_logs ORDER BY id DESC LIMIT 3;"
# 预期：能看到刚才的调用记录，cost > 0

# === 3. 验证 Redis ===
docker exec -it redis redis-cli
> KEYS stats:*                   # 应有多组 {date} key
> GET stats:20260805:total_tokens  # 非零
> GET stats:20260805:total_cost    # 非零
> KEYS quota:*                   # 应有 key 级额度 key

# === 4. 异常测试 ===

# 4a. 无 API Key → 401
curl -s -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen2.5-72b","messages":[{"role":"user","content":"hi"}]}' | jq .
# 预期: {"code":401,"message":"无效的 API Key","data":null}

# 4b. 无效 API Key → 401
curl -s -X POST http://localhost:8080/api/chat \
  -H "Authorization: Bearer sk-invalid-key-xxxxx" \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen2.5-72b","messages":[{"role":"user","content":"hi"}]}' | jq .

# 4c. 模型不存在 → 404
curl -s -X POST http://localhost:8080/api/chat \
  -H "Authorization: Bearer sk-example-key-for-demo-20260805" \
  -H "Content-Type: application/json" \
  -d '{"model":"nonexistent-model","messages":[{"role":"user","content":"hi"}]}' | jq .
# 预期: {"code":404,"message":"模型不存在或已下线","data":null}

# 4d. 缺 model → 422
curl -s -X POST http://localhost:8080/api/chat \
  -H "Authorization: Bearer sk-example-key-for-demo-20260805" \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"hi"}]}' | jq .
# 预期: {"code":422,"message":"参数校验失败","data":{"errors":[...]}}

# 4e. 缺 messages → 422
curl -s -X POST http://localhost:8080/api/chat \
  -H "Authorization: Bearer sk-example-key-for-demo-20260805" \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen2.5-72b"}' | jq .

# === 5. 模拟器验证 ===
# 等待 15 秒后，再次查询 api_usage_logs，应有模拟器生成的额外记录
docker exec -it mysql mysql -u root -proot123 ai_gateway \
  -e "SELECT COUNT(*) FROM api_usage_logs;"
# 预期：> 1（你手动调用 1 次 + 模拟器自动若干次）
```

### 验收标准（核心阶段，必须全部通过）

- [ ] API Key 校验：有效 Key 通过、无效 Key 返回 401
- [ ] 额度校验：超限返回 429（手动调低配额阈值验证）
- [ ] 模型路由：不存在或已下线模型返回 404
- [ ] 参数校验：缺 model / messages 返回 422
- [ ] 成功调用返回 OpenAI 兼容格式（含 `usage.cost`）
- [ ] 调用后 `api_usage_logs` 表有对应记录（含 cost 字段 > 0）
- [ ] 调用后 Redis 当日 Key 全部正常更新
- [ ] 模拟器启动后自动生成数据，MySQL + Redis 均可验证
- [ ] Ctrl+C 后模拟器优雅停止

---

## 阶段 5：统计接口开发

### 阶段目标

实现 Dashboard 所需的全部统计 API，返回正确的实时数据。

### 实现内容

**1. Stats Service — `internal/service/stats_service.go`**

| 方法 | 数据来源 | 说明 |
|---|---|---|
| `GetOverview()` | Redis today + MySQL SUM total + 昨日对比计算 trend | 今日/累计 Token、请求数、成功率、延迟、费用 |
| `GetModelUsage()` | Redis `stats:{date}:model:{id}:*` | 各模型 Token + 占比 + 费用 |
| `GetTrend(days)` | MySQL `GROUP BY DATE(created_at)` 查 api_usage_logs | 近 N 天每日 Token/请求汇总 |

**2. Log Service — `internal/service/stats_service.go`（同文件）**

| 方法 | 说明 |
|---|---|
| `GetRealtimeLogs(page, pageSize, modelID, status)` | 分页查询 api_usage_logs，JOIN models/projects 取名称 |

**3. Model Service — `internal/service/model_service.go`**

| 方法 | 说明 |
|---|---|
| `GetModels()` | 查询 models 表 + 从 Redis 注入 today_tokens、today_cost、today_requests、avg_latency_ms |

**4. Handler 层**

| 文件 | 接口 |
|---|---|
| `handler/stats.go` | GET /api/stats/overview, GET /api/stats/model-usage, GET /api/stats/trend |
| `handler/log.go` | GET /api/realtime/logs |
| `handler/model.go` | GET /api/models |
| `handler/health.go` | GET /api/health（增强：模型状态 + alerts） |

**5. Health Handler 增强**

- 从 models 表查询所有模型状态
- 按架构文档 [5.2 节] 评估：success_rate ≥ 95% → online, ≥ 80% → degraded, else → offline
- 生成 alerts 数组

### 为什么这样设计

- Service 层与阶段 4 的采集链路解耦，只负责从 Redis/MySQL 读取和计算
- 平均成功率和平均延迟为「计算字段」，不直接存储，保证数据一致性
- 所有接口返回格式与阶段 5 前端预期完全对齐

### 涉及文件

| 文件 | 操作 |
|---|---|
| `server/internal/service/stats_service.go` | **新增** |
| `server/internal/service/model_service.go` | **新增** |
| `server/internal/handler/stats.go` | **新增** |
| `server/internal/handler/log.go` | **新增** |
| `server/internal/handler/model.go` | **新增** |
| `server/internal/handler/health.go` | **修改**：增强模型统计 + alerts |
| `server/internal/router/router.go` | **修改**：注册统计路由 |

### API 变化

| 接口 | 方法 | 状态 | 说明 |
|---|---|---|---|
| GET /api/stats/overview | GET | **新增** | 核心概览 |
| GET /api/stats/model-usage | GET | **新增** | 模型用量分布 |
| GET /api/stats/trend | GET | **新增** | 7 天趋势 |
| GET /api/realtime/logs | GET | **新增** | 实时日志（分页） |
| GET /api/models | GET | **新增** | 模型列表 + 今日指标 |
| GET /api/health | GET | **增强** | 增加模型统计 + alerts |

所有响应 Schema 严格遵循架构文档 [4.4.3] 和 [4.4.4] 节。

### 测试方案

```bash
# 确保模拟器已运行至少 2 分钟

# 1. Overview
curl -s http://localhost:8080/api/stats/overview | jq .
# 验证：today.total_tokens > 0, today.total_cost > 0, model_count.online > 0

# 2. Model Usage
curl -s http://localhost:8080/api/stats/model-usage | jq .
# 验证：models 数组 5 个元素，percentage 之和 ≈ 1.0
# 验证：total_tokens = sum(models[].tokens)

# 3. Trend
curl -s http://localhost:8080/api/stats/trend | jq .
# 验证：points 数组长度 ≤ 7

# 4. Realtime Logs
curl -s "http://localhost:8080/api/realtime/logs?page=1&page_size=5" | jq .
# 验证：pagination.total > 0, logs 数组有记录

# 5. Models
curl -s http://localhost:8080/api/models | jq .
# 验证：models 数组 5 个，每个有 today_tokens, today_cost

# 6. Health (增强)
curl -s http://localhost:8080/api/health | jq .
# 验证：online_models / total_models 正确，如有 degraded 模型则 alerts 非空
```

### 验收标准

- [ ] 全部 6 个接口返回 200
- [ ] 返回 Schema 与架构文档定义一致
- [ ] 费用字段在 overview、models 接口中正确返回
- [ ] overview 的 total_tokens ≈ model-usage 的 sum(models[].tokens)（允许微小时间差）
- [ ] 分页功能正常（page、page_size、total）
- [ ] 健康检查增强：模型状态统计正确

---

## 阶段 6：Vue Dashboard 开发

### 阶段目标

完成前端单页 Dashboard，包含所有必需模块 + 调用测试区。

### 实现内容

**页面布局：**

```
┌─────────────────────────────────────────────────────┐
│  [HealthBar]  ● 4/5 在线 │ 最后更新: 14:32:15     │
├─────────────────────────────────────────────────────┤
│  [MetricCards]                                       │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──┐│
│  │¥费用 │ │Token │ │累计  │ │请求数│ │成功率│ │..││
│  │18.52 │ │1.2M  │ │892M  │ │3842  │ │98.3% │ │  ││
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──┘│
├─────────────────────────────────────────────────────┤
│  [ModelCards]                     [PieChart]         │
│  ┌─────────────────┐ ┌─────────────────┐            │
│  │ Qwen2.5-72B  ●  │ │ DeepSeek-V3  ●  │  模型用量   │
│  │ ¥3.82  ████░░  │ │ ¥1.20  ██░░░░  │  环形图     │
│  │ 380ms  98.5%   │ │ 220ms  99.1%   │            │
│  └─────────────────┘ └─────────────────┘            │
├─────────────────────────────────────────────────────┤
│  [TrendChart]  7 天 Token / 费用趋势                  │
│  ┌─────────────────────────────────────────────────┐│
│  │  📈 折线图（双 Y 轴: Token + 费用）              ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  [RealtimeLog]  实时调用流                           │
│  ┌─────────────────────────────────────────────────┐│
│  │ 时间 │ 项目 │ 模型 │ Token │ 费用 │ 状态 │ 延迟 ││
│  │ ...  │ ...  │ ...  │ ...   │ ...  │ ...  │ ...  ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  [TestPanel]  API 调用测试区                          │
│  ┌──────────────────────┬──────────────────────────┐│
│  │ 模型: [下拉选择]     │ 返回结果:                ││
│  │ 消息: [输入框    ]   │ {"choices":[...],        ││
│  │      [发送按钮]      │  "usage":{...}}          ││
│  └──────────────────────┴──────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

**组件清单：**

| 组件 | 文件 | 功能 |
|---|---|---|
| HealthBar | `layout/HealthBar.vue` | 在线模型数/总数、异常提示、最后更新时间 |
| MetricCards | `dashboard/MetricCards.vue` | 6 个指标卡片，费用首项突出，数字动画 |
| ModelCards | `dashboard/ModelCards.vue` | 5 张模型卡片，状态灯 + 进度条 + 费用 |
| TrendChart | `dashboard/TrendChart.vue` | ECharts 折线图，7 天 Token + 费用双线 |
| PieChart | `charts/PieChart.vue` | 环形图，Token 占比（可切换费用维度） |
| RealtimeLog | `dashboard/RealtimeLog.vue` | Element Plus Table，自动滚动 |
| TestPanel | `dashboard/TestPanel.vue` | 模型选择 + 消息输入 + 发送 + 结果展示 |
| StatusDot | `common/StatusDot.vue` | 绿/黄/红状态指示灯 |
| TokenBar | `common/TokenBar.vue` | Token 用量进度条 |

**数据层：**

| 文件 | 功能 |
|---|---|
| `api/index.ts` | axios 实例，baseURL 配置 |
| `api/stats.ts` | 封装 6 个 API 调用函数 |
| `stores/dashboard.ts` | Pinia Store：overview / modelUsage / trend / logs / models 状态 |
| `composables/usePolling.ts` | 通用轮询 Hook，3~5 秒间隔，页面隐藏时暂停 |
| `composables/useCountUp.ts` | 数字平滑增长动画（ease-out） |
| `types/index.ts` | TypeScript interface：OverviewData, ModelUsage, TrendPoint, LogEntry, ModelInfo |

### 为什么这样设计

- 组件细粒度拆分，按模块独立开发调试
- 调用测试区是面试演示的**高光时刻**：现场发一次请求，所有数字自动跳动
- Pinia Store 集中管理状态，轮询 hook 解耦，未来升级 WebSocket 只需改 usePolling
- ECharts 封装为通用组件（LineChart/PieChart），dashboard 组件只传数据

### 涉及文件

| 文件 | 操作 |
|---|---|
| `web/src/App.vue` | **修改**：组装 Dashboard 布局 |
| `web/src/main.ts` | **修改**：注册 Pinia + Router + Element Plus |
| `web/src/router/index.ts` | **新增** |
| `web/src/types/index.ts` | **新增** |
| `web/src/api/index.ts` | **新增** |
| `web/src/api/stats.ts` | **新增** |
| `web/src/stores/dashboard.ts` | **新增** |
| `web/src/composables/usePolling.ts` | **新增** |
| `web/src/composables/useCountUp.ts` | **新增** |
| `web/src/components/layout/HealthBar.vue` | **新增** |
| `web/src/components/dashboard/MetricCards.vue` | **新增** |
| `web/src/components/dashboard/ModelCards.vue` | **新增** |
| `web/src/components/dashboard/RealtimeLog.vue` | **新增** |
| `web/src/components/dashboard/TrendChart.vue` | **新增** |
| `web/src/components/dashboard/TestPanel.vue` | **新增** |
| `web/src/components/common/StatusDot.vue` | **新增** |
| `web/src/components/common/TokenBar.vue` | **新增** |
| `web/src/components/charts/LineChart.vue` | **新增** |
| `web/src/components/charts/PieChart.vue` | **新增** |

### 测试方案

```bash
# 确保后端运行 + 模拟器已运行 2 分钟以上

cd web && npm run dev

# 浏览器打开 http://localhost:5173

# 逐一验证：
# 1. 顶部 HealthBar → 显示 "4/5 在线"（如 GLM-4 偶发降级）
# 2. MetricCards → 6 个数字卡片，"今日费用"在首位突出显示
# 3. ModelCards → 5 张卡片，状态灯颜色正确
# 4. RealtimeLog → 日志表格有数据，自动滚动
# 5. TrendChart → 折线图有数据点
# 6. PieChart → 环形图 5 个扇区
# 7. TestPanel → 
#    a. 下拉选择模型
#    b. 输入消息
#    c. 点击发送
#    d. 右侧显示返回结果（回复 + Token + 费用）
#    e. MetricCards 数字自动更新
#    f. RealtimeLog 出现刚才的调用记录
```

### 验收标准

- [ ] 页面无报错、无白屏
- [ ] 所有 8 个模块渲染正常
- [ ] 轮询刷新有效（3~5 秒数字更新）
- [ ] **调用测试区闭环完整**（发请求 → 结果展示 → 指标更新 → 日志追加）
- [ ] 1920×1080 分辨率下布局合理
- [ ] 响应式：1366×768 笔记本最小可用

---

## 阶段 7：系统完善与交付

### 阶段目标

抛光工程细节，准备交付物。

### 实现内容

**1. README.md**

```markdown
# 企业大模型公共服务平台 (AI Gateway Console)

## 项目简介
轻量级企业大模型 API 管理平台（模拟版）。
支持多模型统一管理、API 调用全链路日志、Token/费用实时统计与 Dashboard 展示。

## 技术栈
- 后端：Go + Gin + Gorm + MySQL + Redis
- 前端：Vue3 + Vite + Element Plus + ECharts

## 快速启动
### Docker Compose（推荐）
docker-compose up -d
# 后端: http://localhost:8080
# 前端: http://localhost:3000

### 本地开发
# 后端
cd server && go run cmd/server/main.go

# 前端
cd web && npm install && npm run dev

## Demo 实现范围
详见架构文档 1.3 节

## 演示步骤
1. docker-compose up -d
2. 打开浏览器 → Dashboard 首页
3. 在调用测试区发送一次请求
4. 观察指标卡片数字变化 + 实时日志更新
5. 讲解架构设计思路

## 接口文档
详见架构文档 4.4 节
```

**2. DEMO_CHECKLIST.md**

面试演示操作清单，按步骤列出操作和验证点。

**3. Docker 优化**

- 验证 `docker-compose up -d` 一键启动完整流程
- 检查容器健康检查配置
- 前端 nginx.conf 正确代理后端 API

**4. 错误处理收尾**

- 404 Not Found 页面（Vue Router catch-all）
- 统一错误处理边界覆盖

**5. CHANGELOG.md**

```
# Changelog

## V1.2 (2026-08)
- 完整 Dashboard 实现
- POST /api/chat 核心调用闭环
- 费用统计为核心指标
- API Key 安全（bcrypt + 额度校验）
- Docker Compose 一键启动
```

### 涉及文件

| 文件 | 操作 |
|---|---|
| `README.md` | **新增** |
| `DEMO_CHECKLIST.md` | **新增** |
| `CHANGELOG.md` | **新增** |
| `docker-compose.yml` | **修改**：最终验证优化 |
| `server/Dockerfile` | **修改**：如需要 |
| `web/Dockerfile` | **修改**：如需要 |
| `web/nginx.conf` | **修改**：代理配置 |

### 验收标准

- [ ] README 按步骤可成功启动项目
- [ ] `docker-compose up -d` 一键完整启动
- [ ] Demo 检查清单覆盖全部演示环节
- [ ] 无已知未修复 bug
- [ ] CHANGELOG 记录完整

---

## 附录 A：依赖关系

```
阶段 0 ──→ 阶段 1 ──→ 阶段 2 ──→ 阶段 3 ──→ 阶段 4 ──→ 阶段 5 ──→ 阶段 6 ──→ 阶段 7
  │          │          │          │           │           │           │
  确认       骨架       建表      Gin启动     POST /api/   统计API     Dashboard
  计划       环境       +Seed     +Health      chat闭环                +测试区
```

阶段 1~2 可部分并行（目录结构 + 建表 SQL 可同时）；阶段 3 起严格串行。

---

## 附录 B：关键风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 阶段 4 bcrypt 性能 | 每次 API Key 校验 ~50ms | Demo 单机可接受；生产可加 Key→project 内存缓存 |
| 模拟回复内容质量 | 可能看起来不像 LLM 输出 | 准备 20+ 条预设模板，按消息主题简单匹配 |
| ECharts 集成兼容 | vue-echarts 版本升级 Breaking | 锁定 vue-echarts 6.x + echarts 5.x |
| Redis 数据为空 | 刚启动无数据，Dashboard 报错 | Service 层防御性编程，空值返回 0/默认值 |
| 阶段 6 开发量较大 | 组件多，时间紧 | 优先保证核心模块（MetricCards + TestPanel + ModelCards），其他可先简后精 |

---

## 附录 C：Demo 数据预设

为便于演示，以下数据在种子数据中预置：

| 类型 | 数量 | 内容 |
|---|---|---|
| 用户 | 3 | admin / teacher_wang / student_zhang |
| 项目 | 1 | AI 智能问答系统 |
| API Key | 1 | Prefix: `sk-example-k`（原始 Key 记录在注释中） |
| 模型 | 5 | Qwen2.5-72B, DeepSeek-V3, MiniMax-Text-01, GLM-4, Qwen2.5-Coder |
| 日配额 | 1,000,000 tokens/Key/天 | 可在 config.yaml 调整 |

---

## 开始条件

以上为完整的《项目分阶段开发计划》。

**当前状态：阶段 0 —— 等待确认。**

请回复：
- **"同意，开始阶段 1"** → 开始项目初始化
- 对某阶段有疑问 → 先澄清再启动
- 需要调整阶段划分 → 讨论修改

不写任何代码。
